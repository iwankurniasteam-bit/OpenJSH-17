# OpenJSH-17
OpenJSH java shell. requires Java 17 ONLY. older/newer java versions are NOT supported

# How to run

If you have Java 17 installed. you can run OpenJSH

Step 1: Clone the repo (via git clone)
Step 2: Change dir to OpenJSH-17
Step 3: Type 'java -jar OpenJSH.jar'
